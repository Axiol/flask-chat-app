# Sample Flask chat app
Nothing super fancy, just a quick chat app to learn the basic of Flask and Socket.io

# Installation
Install the two dependencies 
```
pip install flask flask-socketio
```
And run the `chat.py` file
```
python chat.py
```